exports.init = function(app) {
/*
    var home = require('./home');
    app.all('/home', home.index);
    var textApi = require('./api/text');
    app.get('/api/texts', textApi.list);
    app.get('/api/texts/:area_code', textApi.findByAreaCode);

these aren't used. the routes in ../app.js are used.
*/
};
